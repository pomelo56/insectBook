# 昆虫ID系统重构测试指南

本指南提供了对昆虫ID系统重构后功能的全面测试步骤，确保所有功能正常运行。

## 测试前准备

1. **部署云函数**
   - 确保已部署以下修改后的云函数：
     - `syncInsectData` - 支持生成和保存externalId
     - `getInsectDetail` - 支持通过externalId查询
     - `getInsectList` - 返回externalId字段
     - `migrateInsectExternalIds` - 运行一次数据迁移

2. **运行数据迁移**
   - 通过小程序管理后台执行`migrateInsectExternalIds`云函数
   - 确认所有现有昆虫数据都已添加externalId

## 功能测试

### 1. 昆虫列表页面测试

- **测试点**：验证列表数据是否包含externalId
- **操作步骤**：
  1. 打开小程序首页
  2. 检查控制台日志，确认每个昆虫项都包含externalId
  3. 确认列表显示正常，没有UI异常

### 2. 昆虫详情页导航测试

- **测试点**：验证使用externalId导航到详情页的功能
- **操作步骤**：
  1. 从首页点击任意昆虫项
  2. 观察控制台，确认URL中传递的是externalId参数
  3. 确认详情页正确加载

### 3. 昆虫详情查询测试

- **测试点**：验证通过externalId查询昆虫详情的功能
- **操作步骤**：
  1. 在详情页检查请求参数，确认使用externalId查询
  2. 验证返回的详情数据包含externalId
  3. 测试直接通过insectId和name查询的兼容性

### 4. 数据完整性测试

- **测试点**：确保所有数据字段完整保留
- **操作步骤**：
  1. 检查每个昆虫详情页的所有信息是否完整
  2. 验证图片、描述、发现记录等数据是否正确显示
  3. 确认没有数据丢失或显示异常

### 5. 新建昆虫测试

- **测试点**：验证新添加的昆虫自动生成externalId
- **操作步骤**：
  1. 通过小程序添加新的昆虫记录
  2. 检查数据库中是否自动生成了externalId
  3. 验证新昆虫可以通过externalId正常查询和访问

## 错误处理测试

### 1. 空externalId处理

- **测试点**：验证系统对空或无效externalId的处理
- **操作步骤**：
  1. 尝试通过不存在的externalId访问详情页
  2. 确认系统能够优雅地处理错误并提示用户

### 2. 兼容性测试

- **测试点**：确保系统兼容旧的查询方式
- **操作步骤**：
  1. 尝试通过旧的中文ID访问昆虫详情
  2. 确认系统仍然能够正确识别并返回数据

## 性能测试

- **测试点**：验证查询性能是否受到影响
- **操作步骤**：
  1. 测量页面加载时间
  2. 比较重构前后的性能差异
  3. 确保性能没有显著下降

## 测试检查清单

- [ ] 云函数部署完成
- [ ] 数据迁移执行成功
- [ ] 昆虫列表正确显示并包含externalId
- [ ] 从列表到详情页的导航使用externalId
- [ ] 详情页查询使用externalId
- [ ] 所有数据完整显示
- [ ] 新建昆虫自动生成externalId
- [ ] 错误处理正常工作
- [ ] 系统兼容旧的查询方式
- [ ] 性能符合预期

## 问题排查

如果测试中发现问题，请检查以下几点：

1. 云函数是否正确部署并包含最新代码
2. 数据迁移是否成功执行，所有记录是否都有externalId
3. 小程序代码是否已更新并重新部署
4. 检查控制台日志以获取详细错误信息

如有其他问题，请联系开发人员协助解决。